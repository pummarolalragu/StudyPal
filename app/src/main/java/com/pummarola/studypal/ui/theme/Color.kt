package com.pummarola.studypal.ui.theme

import androidx.compose.ui.graphics.Color

// --- NUOVI COLORI MINIMALISTI E ACCENTI SOBRI ---

// Colori di base (Bianco/Nero/Grigio)
val BlackText = Color(0xFF121212)
val LightGrayBackground = Color(0xFFF7F7F7) // Sfondo quasi bianco
val GrayCard = Color(0xFFEBEBEB)
val White = Color(0xFFFFFFFF)

// Colore d'ACCENTO Unico (Scegliamo un Blu tenue e moderno)
val AccentBlue = Color(0xFF007AFF) // Classico blu Apple
val AccentLightBlue = Color(0xFFD0E8FF) // Versione chiara dell'accento

// Colori di stato (Usati solo per i badge/feedback)
val LiveGreen = Color(0xFF34C759) // Verde per "IN CORSO"
val FutureBlue = Color(0xFF007AFF) // Blu per "PROGRAMMATA"
val AlertRed = Color(0xFFFF3B30) // Rosso per "Esci"

//Colori NEON
val NeonPink = Color(0xFFF50057)
val NeonCyan = Color(0xFF00E5FF)
val NeonYellow = Color(0xFFFFD600)